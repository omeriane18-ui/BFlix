# ربط BFlix بحاوية JSONBin — دليل الإعداد

هذا الملف يشرح ما تغيّر في المشروع، وكيف تُنهي الإعداد خلال دقائق.

---

## ⚠️ افعل هذا أولًا: أبطِل مفتاحك الحالي

المفتاح الذي شاركته (`$2a$10$sfbHlW7rEN...`) هو **Master Key**، أي مفتاح حسابك كله على JSONBin: من يملكه يستطيع قراءة وتعديل وحذف كل حاوياتك، لا هذه الحاوية فقط. صار الآن مكشوفًا خارج جهازك.

1. ادخل [jsonbin.io](https://jsonbin.io) → **API Keys**.
2. أعد توليد الـ Master Key (Regenerate) لإبطال القديم.
3. من نفس الصفحة أنشئ **Access Key** جديدًا، وامنحه صلاحيتَي **Bins Read** و **Bins Update** فقط.
4. استخدم الـ Access Key في لوحة التحكم — لا الـ Master Key.

---

## 1️⃣ خطأ `Bin cannot be blank`

JSONBin يرفض إنشاء حاوية بجسم فارغ أو بـ `[]` وحدها. أنشئها بمحتوى ابتدائي صالح.

افتح `initial-bin.json` الموجود في الحزمة، وانسخ محتواه كاملًا والصقه في محرّر الحاوية عند الإنشاء. أو، إن أردت البدء من الصفر، الصق هذا:

```json
{
  "items": [],
  "updatedAt": "2026-01-01T00:00:00.000Z"
}
```

ثم اضغط **Create**، واجعل الحاوية **Public** من إعداداتها (`Private: false`) — هذا ما يسمح للزوّار بالقراءة دون أي مفتاح.

> إن كنت ستستخدم نفس الحاوية الحالية `6aaf01aeac6210605ae0ec3c`، يكفي أن تحدّث محتواها وتجعلها Public.

---

## 2️⃣ خطأ CORS ومفاتيح الطلبات

ما رأيته لم يكن حظرًا حقيقيًا من CORS في أغلب الحالات. JSONBin يدعم الطلبات المباشرة من المتصفح (`GET` و `PUT`) ويرسل ترويسات CORS الصحيحة. ما يظهر في الـ Console كـ "CORS error" ينتج عادة عن أحد هذه الأسباب:

| السبب | العلاج في هذه النسخة |
|---|---|
| الحاوية Private والقراءة بلا مفتاح → 401 | اجعل الحاوية Public، فالقراءة تصبح بلا مفتاح |
| ترويسة خاطئة (`X-Master-Key` مقابل `X-Access-Key`) | `cloud.js` يختار الترويسة تلقائيًا حسب شكل المفتاح |
| نسيان `Content-Type: application/json` في `PUT` | مُرسَلة دائمًا الآن |
| إرسال مصفوفة عارية في الجسم | يُرسل الكود كائنًا `{ items: [...] }` |

كل خطأ الآن يُعرض لك بنصّه الحقيقي ورقم حالته (401، 403، 404…) بدل رسالة عامة.

**نقطة يجب أن تعرفها:** أي مفتاح تضعه داخل صفحة يفتحها المتصفح يمكن قراءته. لهذا لا يوجد مفتاح داخل ملفات المشروع إطلاقًا — أنت تُدخل مفتاح الكتابة يدويًا في لوحة التحكم، ويُحفظ في متصفحك أنت فقط (`localStorage`). الزوّار يقرؤون بلا مفتاح.

إن أردت لاحقًا إخفاء المفتاح تمامًا، فالحل الوحيد هو خادم وسيط (Cloudflare Worker أو Netlify Function مجانًا) يحتفظ بالمفتاح ويمرّر الطلبات. GitHub Pages وحده لا يستطيع ذلك لأنه استضافة ملفات ثابتة.

---

## 3️⃣ تعارض الذاكرة المحلية مع السحابة

كان الكود يحفظ محليًا عند فشل الرفع، فتظهر إضافاتك عندك وحدك. هذا السلوك أُزيل:

- **القراءة:** كل صفحة تجلب البيانات من JSONBin عند فتحها. `localStorage` صار نسخة عرض احتياطية عند انقطاع الإنترنت، لا مصدرًا للحقيقة.
- **الكتابة:** الحفظ ينجح في السحابة أولًا، ثم تتحدّث الواجهة. إذا فشل الرفع، **لا يُطبَّق التغيير محليًا** وتظهر لك رسالة بسبب الفشل. لن تعود تظن أن شيئًا حُفظ وهو لم يُحفظ.
- في أعلى اللوحة شريط حالة يقول بوضوح: متصل بالسحابة، أم يعرض نسخة محفوظة.

---

## خطوات التشغيل

1. افتح `cloud.js` وتأكد أن `BIN_ID` هو معرّف حاويتك.
2. ارفع الملفات إلى المستودع وانشرها على GitHub Pages.
3. افتح `admin.html` → كلمة المرور `bflix2026`.
4. في **إعدادات السحابة**: الصق الـ Access Key ← **حفظ المفتاح** ← **اختبار الاتصال**.
5. إن كانت لديك بيانات في هذا المتصفح من قبل، اضغط **رفع بيانات هذا المتصفح إلى السحابة** مرة واحدة.
6. أضف عملًا جديدًا، ثم افتح الموقع من هاتفك أو من متصفح خفي للتأكد أنه يظهر.

---

## ما يزال ناقصًا

شاشة الدخول في `admin.html` حماية شكلية: كلمة المرور مكتوبة في المتصفح، وأي شخص يفتح أدوات المطوّر يتجاوزها. هي تمنع الفضول العابر فقط. مفتاح الكتابة هو الحاجز الحقيقي الوحيد حاليًا — احرص ألّا تفتح اللوحة على جهاز مشترك، وامسح المفتاح من الحقل إن فعلت.

للحصول على مصادقة حقيقية، الانتقال إلى Supabase (قاعدة بيانات + تسجيل دخول + صلاحيات على مستوى الصف) هو الخطوة المنطقية التالية.
